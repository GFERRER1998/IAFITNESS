spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./src/main/resources/config/

server:
  port: 8888
